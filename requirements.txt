# ===================================================
# Python Package Requirements for NexusCore
# バージョン制約の方針:
#   - メジャーバージョン: 破壊的変更を防ぐため上限を設定
#   - マイナーバージョン: セキュリティ修正を受けられるよう下限のみ
# ===================================================

# --- Core AI and Machine Learning ---
google-generativeai>=0.4.0,<1.0.0
scipy>=1.11.0,<2.0.0

# --- Testing Framework ---
# pytest 9系: PYSEC-2026-1845 (7.4.4 未満に既知脆弱性・Fix 9.0.3) 対応・2026-08-29 A群(3)
pytest>=9.0.3,<10.0.0
pytest-cov>=4.1.0,<5.0.0
anyio>=4.0.0,<5.0.0  # Mainブランチの修正を採用（正しいパッケージ名）
pytest-mock>=3.12.0,<4.0.0

# --- Web UI and Server ---
gradio>=4.16.0,<7.0.0  # <5.0.0→<7.0.0: gradio 4.xは古いhuggingface_hub API(HfFolder)に依存しCI非互換・6.xは.venv実証済み(2026-07-09)
fastapi>=0.110.0,<1.0.0 # Mainの新しいバージョンを採用
uvicorn>=0.27.0,<1.0.0  # Mainの新しいバージョンを採用
streamlit>=1.29.0,<2.0.0
Flask>=3.0.0,<4.0.0     # あなたのPRのアップグレードを採用
Flask-SQLAlchemy>=3.1.0,<4.0.0
Flask-Migrate>=4.0.0,<5.0.0
# Flask-CORS 6系: PYSEC-2026-1383/-1384/-1385 (4.0.2 に既知脆弱性・Fix 6.0.0) 対応・2026-08-29 A群(3)
Flask-CORS>=6.0.0,<7.0.0
Flask-Login>=0.6.0,<1.0.0
authlib>=1.3.0,<2.0.0
celery>=5.3.0,<6.0.0
redis>=5.0.0,<6.0.0
pydantic>=2.0.0,<3.0.0  # For data validation and settings management
matplotlib>=3.8.0,<4.0.0

# --- Audio Processing ---
# 2026-08-29 A群(4): SpeechRecognition は実import 0件で削除
# pydub/standard-aifc は維持 — gradio が import 時に pydub を要求する実行時推移依存
# (pytest コレクションエラー4件で実測・直接import 0件でも削除不可)
pydub>=0.25.0,<1.0.0
standard-aifc; python_version < '3.13'
sounddevice>=0.4.6,<1.0.0

# --- Translation ---
google-cloud-translate>=3.15.0,<4.0.0
patch>=1.16.0,<2.0.0 # library used by patch_applier

# --- Utilities ---
GitPython>=3.1.40,<4.0.0  # guardian_agent.py等が直接import git（CI収集エラー解消・2026-08-15・lock側は3.1.45）
httpx>=0.25.0,<1.0.0
httpcore>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0  # For Slack notifications
python-dotenv>=1.0.0,<2.0.0

termcolor>=2.4.0,<3.0.0
pyyaml>=6.0.0,<7.0.0  # For test strategy config and constitution file

# --- Authentication (新規追加) ---
pyjwt>=2.8.0,<3.0.0  # JWT認証用（タスク2で使用）

# --- CLI Framework ---
click>=8.0.0,<9.0.0  # CLI subcommand framework
