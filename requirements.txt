# ===================================================
# Python Package Requirements for NexusCore (Final Corrected Version)
# ===================================================

# --- Core AI and Machine Learning ---
# OpenAI SDK: 1.x 系を前提（破壊的変更を避けるため 2.0 未満に固定）
openai>=1.30.0,<2.0.0
# CPU-only PyTorch to avoid pulling large CUDA dependencies in CI
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
# TensorFlow: 2.x 系を前提（3.0 未満に固定）
tensorflow>=2.14.0,<3.0.0
# Google Generative AI: 0.x 系を前提（1.0 未満に固定）
google-generativeai>=0.4.0,<1.0.0
scipy>=1.10.0,<2.0.0  # For audio processing, missing from previous version

# --- Testing Framework ---
# Note: テストツールは requirements-dev.txt にも含まれていますが、
# CI/CD やランタイムでのテスト実行を考慮して requirements.txt にも含めています
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
anyio>=4.0.0,<5.0.0  # pytest-anyio ではなく anyio が正しいパッケージ名
pytest-mock>=3.12.0,<4.0.0

# --- Web UI and Server ---
# Gradio: 4.x 系を前提（5.0 未満に固定）
gradio>=4.16.0,<5.0.0
# FastAPI: 0.x 系を前提（1.0 未満に固定）
fastapi>=0.110.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
websockets>=12.0,<13.0
streamlit>=1.28.0,<2.0.0
# Flask: 2.x 系を前提（3.0 未満に固定）
Flask>=2.2.0,<3.0.0
Flask-SQLAlchemy>=3.0.0,<4.0.0
Flask-Migrate>=4.0.0,<5.0.0
Flask-CORS>=4.0.0,<5.0.0  # CORS support for external API
authlib>=1.2.0,<2.0.0
# Celery: 5.x 系を前提（6.0 未満に固定）
celery>=5.3.0,<6.0.0
redis>=5.0.0,<6.0.0  # Celery broker/backend
matplotlib>=3.7.0,<4.0.0

# --- Audio Processing ---
SpeechRecognition>=3.10.0,<4.0.0
pydub>=0.25.0,<1.0.0
sounddevice>=0.4.6,<1.0.0
standard-aifc; python_version < '3.13'
patch>=1.16.0,<2.0.0  # library used by patch_applier

# --- Translation ---
google-cloud-translate>=3.12.0,<4.0.0

# --- Code Interpretation & Jupyter ---
# --- Utilities ---
httpx>=0.25.0,<1.0.0
httpcore>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0  # For Slack notifications
python-dotenv>=1.0.0,<2.0.0
termcolor>=2.3.0,<3.0.0
pyyaml>=6.0.0,<7.0.0  # For test strategy config
