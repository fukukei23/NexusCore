# Development dependencies for NexusCore
# 開発ツールのみをまとめたファイル（ランタイム依存は requirements.txt に）

# フォーマッタ / Linter
black>=23.12.0,<25.0.0
ruff>=0.1.0,<1.0.0

# 型チェック
mypy>=1.7.0,<2.0.0

# テスト
# Note: pytest 関連は requirements.txt にも含まれていますが、
# 開発環境でのテスト実行を考慮して requirements-dev.txt にも含めています
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
pytest-mock>=3.12.0,<4.0.0
pytest-timeout>=2.0.0,<3.0.0

# 型付きスタブ（よく使うライブラリ向け）
types-requests>=2.31.0,<3.0.0
types-PyYAML>=6.0.0,<7.0.0

# セキュリティ監査ツール (Phase 0〜)
# Phase 0 ベースラインで導入。SaaS 商用化に向けて継続的に走査する
bandit==1.9.4
semgrep==1.166.0
pip-audit==2.10.1
detect-secrets==1.5.0
