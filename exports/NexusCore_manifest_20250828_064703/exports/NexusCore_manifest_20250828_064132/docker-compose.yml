# ==============================================================================
# ファイル名: docker-compose.yml
# 配置場所: プロジェクトのルートディレクトリ
# 目的: RedisとPostgreSQLをワンコマンドで起動するための設計図
# 使い方: このファイルがあるディレクトリで `docker-compose up -d` を実行
# ==============================================================================
version: '3.8'

services:
  # サービス①: Redis (ステートストア / 作業台のホワイトボード)
  redis:
    image: redis:7.2-alpine
    container_name: nexus_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  # サービス②: PostgreSQL (ナレッジベース / 図書室・保管庫)
  postgres:
    image: postgres:16.1-alpine
    container_name: nexus_postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=nexus_user
      - POSTGRES_PASSWORD=your_strong_password_here # .envファイルで管理することを推奨
      - POSTGRES_DB=nexus_knowledge_base
    restart: unless-stopped

volumes:
  redis_data:
  postgres_data: